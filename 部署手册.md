# 🚌 一键部署手册（宝宝巴士版）

## 你在干嘛？（30 秒读懂）

你的网站会"说话"，声音由服务器上一台机器（Qwen3-TTS 模型）发出。
它现在睡着了，所以网站只能用难听的系统机器人音。

本手册 = 叫醒它。**全程只复制粘贴 1 行命令 + 等几分钟。**

---

## 准备（1 分钟）

1. 打开 autodl.com，登录
2. 在控制台找到你的实例卡片（写着"运行中"的那张）
3. 点卡片上的「**JupyterLab**」按钮 → 打开后找到**黑色大输入框**（那个叫"终端"）

---

## 开始（就这一行，粘贴 → 回车 → 去喝口水）

```
cd /root && (cd countursheep 2>/dev/null && git pull -q || git clone -q https://github.com/babibabiya/countursheep.git) && cd countursheep && bash deploy_tts.sh
```

你会看到：

| 屏幕出现 | 意思 |
|---|---|
| 【第 1/5 步】…【第 2/5 步】… | 正常，它在干活 |
| ...还在加载/下载，别慌 | 第一次要下载 4GB 模型，等 3~15 分钟 |
| 绿色 ✅ 部署成功 | 完事！ |

---

## 验收

手机打开你的网站 → 点播放 → **温柔的 AI 女声** = 大功告成 🎉

想先偷看一眼也行，浏览器打开这个网址：

```
https://u1147881-c7kl-35a69bfe.bjb1.seetacloud.com:8443/health
```

看到 `"model_loaded": true` 就说明它醒了。

---

## 红灯怎么办

| 屏幕上出现 | 怎么办 |
|---|---|
| git clone 超时 / 拉不动 | 先粘贴 `source /etc/network_turbo` 回车，再粘贴上面那行 |
| pip 安装失败 | 同上，先加速再来一次 |
| 没检测到 GPU | 你开的是 CPU 实例，去 AutoDL 换成 GPU 实例 |
| 等了 15 分钟还是 ❌ | 把屏幕最后 20 行复制发给 AI 助手 |

---

## 以后要知道的

- **实例关机后声音服务会停**。下次开机，在终端把那一行命令**再粘贴一次**即可恢复（模型已缓存，约 1 分钟）
- 实例按小时计费，不用时可以关机，代价只是网站暂时没自然语音（AI 文字对话不受影响）
- 想换音色：网站播放页里点"音色"按钮循环切换（温柔女声/甜美女声/沉稳男声…共 5 个）
